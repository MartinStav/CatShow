# Frontend (Quasar / Vite) environment.
# Copy to `.env.development` for local dev, `.env.production` for production builds.

# Base URL of the AdonisJS API (without trailing slash).
# Used by the axios client and the realtime WebSocket composable.
VITE_API_URL=http://localhost:3333/api/v1
